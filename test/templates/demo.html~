<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>merriam webster demo</title>
  </head>
  <body>
    <h2>Sample Metadata:</h2>
    {% for d in l%}
    <table border="1">
      {% for key in d['meta'] %}
      <tr>
        <td>{{key}}</td> <td>{{d['meta'][key]}}</td>
      </tr>
      {% endfor %}
    </table>
    <br>
    {% endfor %}
  </body>
</html>
